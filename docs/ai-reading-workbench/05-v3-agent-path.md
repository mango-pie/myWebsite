# 05 V3：Agent 学习路线（远期）

> 在 V2 搜索与批量能力稳定后实施。

## 1. 目标

用户描述学习目标后，Agent 生成结构化学习路径，并按章节检索资料、进入精读链路，形成可复习的博客 + 知识库专题。

```text
学习目标
  ↓
AI 生成学习大纲（章节）
  ↓
每章搜索候选资料
  ↓
用户确认章节与 URL
  ↓
批量精炼 → 预览 → 发博客 / 入知识库
  ↓
专题归档（可选）
```

## 2. 范围

### 要做

- 学习大纲落库（新表或挂在学习模块）。
- 大纲章节与 `source_document` / `knowledge_note` 关联。
- Agent 按章调用搜索策略，汇总候选供确认。
- 专题知识库或标签归档。

### 不做（除非单独立项）

- 无人值守自动发布博客。
- 完全替代人工预览。
- 复杂多 Agent 协作框架。

## 3. 数据预留建议

可后续新增（V1/V2 不必建）：

```text
learning_path          学习路径
learning_path_item     章节 / 任务
item ↔ source_document / knowledge_note
```

V1 的 `source_type=AGENT` 与搜索日志可直接被 V3 复用。

## 4. 与现有系统关系

- 精读与发布：复用 V1 工作台。
- 搜索：复用 V2 `KnowledgeSearchStrategy`。
- 学习任务：可与现有 Study 模块对接（可选）。

## 5. 验收（远期）

- 输入主题能生成可编辑大纲。
- 按章确认后能批量产出精读 note。
- 专题下可浏览已精炼文章与知识库问答范围。
